# ============================================
# KONFIGURASI APLIKASI POS
# ============================================
# File ini adalah template konfigurasi environment.
# Copy file ini ke .env dan sesuaikan nilainya.

# Port server POS Express.js
PORT=3000

# Bind IP Host server. 
# Gunakan 0.0.0.0 agar server dapat diakses oleh perangkat HP/PC lain di jaringan Wi-Fi/LAN lokal yang sama.
HOST=0.0.0.0

# ============================================
# SECURITY SETTINGS (PENTING!)
# ============================================
# WAJIB DIGANTI! Gunakan string random 64 karakter untuk keamanan maksimal
# Generate dengan perintah:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=GANTI_INI_DENGAN_STRING_RANDOM_64_KARAKTER_UNTUK_KEAMANAN_MAKSIMAL

# Session Timeout dalam milidetik (default: 24 jam = 86400000)
# 1 jam = 3600000, 8 jam = 28800000, 24 jam = 86400000
SESSION_TIMEOUT=86400000

# Database Path (opsional - default: ./database.db)
# DB_PATH=./database.db

# Node Environment (production/development)
NODE_ENV=production
